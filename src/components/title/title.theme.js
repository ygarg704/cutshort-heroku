import { makeStyles } from "@mui/styles";

const useStyle = makeStyles(() => ({
  titleCard: {
    marginTop: "3%",
    marginBottom: "3%",
    alignItems: "center",
    justifyContent: "center",
    fontFamily: "Inter",
  },
}));

export { useStyle };
